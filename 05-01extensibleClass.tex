 \begin{Prob}\upshape\Must
  実行例\ifText4.3\else\ref{ExecconstructorClass}\fi
  のクラスの記述に対して次のことを行いなさい。
  \begin{enumerate}
   \item 実行例\ifText5.3\else\ref{ExecconstructorExtensible}\fi
         のようにオブジェクトの\ElmJ{extensible}属性を変更したときの結果を比較せよ。
   \item クラス\texttt{Person}を\ElmJ{freeze}できるか確認せよ。また、
         \texttt{freeze}後にインスタンスを作成したとき、インスタンスは
         \ElmJ{freeze}されているか確認せよ。
  \end{enumerate}
 \end{Prob}
\ifText 結果は別紙で提出のこと\fi